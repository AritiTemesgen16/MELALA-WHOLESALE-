import React from 'react';

// Production build: Role switcher UI is disabled to prevent end users from seeing demo role controls.
// Real roles are handled via standard user authentication (currentUser.role).
export const RoleSwitcher: React.FC = () => {
  return null;
};
